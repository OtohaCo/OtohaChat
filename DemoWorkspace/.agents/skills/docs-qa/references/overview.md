# Demo workspace

This folder is a public sample for OtohaChat skills.

OtohaChat is a native SwiftUI client for SwiftAgent. It can load skills from an authorized folder's `.agents/skills` directory and read only the files the user has granted.
