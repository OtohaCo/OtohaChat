# DemoWorkspace

Authorize this folder in OtohaChat Settings to run the bundled skills:

- `docs-qa` — search and cite files under `docs/`
- `note-capture` — write a notebook entry after confirmation

These skills contain no secrets and do not run scripts.
