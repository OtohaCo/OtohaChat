# OtohaChatKit

Shared runtime for the OtohaChat macOS and iOS apps.

This module owns conversation lifecycle, provider configuration, Keychain
credentials, catalog refresh, tools, and skill loading. SwiftUI lives in
`Apps/`.
